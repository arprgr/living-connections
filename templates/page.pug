html
  head
    meta(charset="utf-8")
    link(rel="stylesheet", type="text/css", href=stylesheet)
    title= title
    script(src="//ajax.googleapis.com/ajax/libs/angularjs/1.5.8/angular.min.js")
    script.
      var require = {
        baseUrl: "/js",
        paths: {
          "jquery": "lib/jquery-3.1.1.slim.min"
        },
        map: {
          "*": { "jquery": "jquery-private" }, 
          "jquery-private": { "jquery": "jquery" } 
        },
        deps: [ "appui" ], 
        callback: function(App) { new App().open(); } 
      }
    script(src="/js/lib/require.js")
  body
    div(id="startup")
      div(class="inner")
        div(class="logo")
        div(class="under")
          noscript.
            To use Living Connections, you must enable Javascript in your browser.
          div(class="waiting")
          div(class="login")
          div(class="message")
    div(id="app")
      div(class="header")
      div(class="listing")
      div(class="activity")
